object AppVersion {

    const val versionCode = 1

    const val versionName = "0.0.1alpha"

    // Requires 3 numbers, and MAJOR must be > 0 for the desktop DMG/MSI packaging
    const val desktopAppVersion = "1.0.0"

}
